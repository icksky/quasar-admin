interface Settings {
  title: string
  showSidebar: boolean
  showNavbar: boolean
}

const settings: Settings = {
  title: 'Quasar Admin',
  showSidebar: true,
  showNavbar: true,
}

export default settings
